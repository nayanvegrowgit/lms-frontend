import React from "react";
const UserDashboard = ({ userId }) => {
  return;
  <>
    {userId.role === "admin" && <AdminHome />}
    {userId.role === "librarian" && <LibrarianHome />}
    {userId.role === "member" && <MemberHome />}
  </>;
};

export default UserDashboard;
